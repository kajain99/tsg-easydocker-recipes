{
  "name": "radarr",
  "version": 1.02,
  "category": "media",
  "icon": "radarr.svg",
  "description": "Movie manager for automated downloads and library organization",
  "services": {
    "radarr": {
      "container_name": "${RADARR_CONTAINER_NAME}",
      "image": "${RADARR_IMAGE}",
      "environment": {
        "PUID": "${PUID}",
        "PGID": "${PGID}",
        "TZ": "${TZ}"
      },
      "volumes": [
        "${RADARR_CONFIG_LOCATION}:${RADARR_CONFIG_TARGET}",
        "${DOWNLOADS_LOCATION}:${RADARR_DOWNLOADS_TARGET}",
        "${MOVIES_LOCATION}:${RADARR_MOVIES_TARGET}"
      ],
      "ports": [
        "${RADARR_PORT}:${RADARR_INTERNAL_PORT}"
      ],
      "restart": "${RESTART_POLICY}",
      "network_mode": "${RADARR_NETWORK_MODE_MODE}",
      "networks": "${RADARR_NETWORK_MODE_SERVICE_NETWORKS}"
    }
  },
  "networks": "${RADARR_NETWORK_MODE_DEFINITIONS}",
  "app_links": [
    {
      "label": "Open Radarr",
      "service": "radarr",
      "port": "${RADARR_PORT}",
      "path": "/",
      "scheme": "http"
    }
  ],
  "fields": [
    {
      "name": "RADARR_PORT",
      "label": "R1: App Port",
      "section": "required",
      "input_type": "text",
      "required": true,
      "default": "7878",
      "help": "Port used to open Radarr in your browser."
    },
    {
      "name": "RADARR_CONFIG_LOCATION",
      "label": "R2: Radarr Config Folder",
      "section": "required",
      "input_type": "text",
      "required": true,
      "default": "./radarr",
      "help": "Stores Radarr configuration, database, and application state."
    },
    {
      "name": "DOWNLOADS_LOCATION",
      "label": "R3: Downloads Folder",
      "section": "required",
      "input_type": "text",
      "required": true,
      "default": "./downloads",
      "help": "Folder where your download client saves completed and in-progress movie downloads."
    },
    {
      "name": "MOVIES_LOCATION",
      "label": "R4: Movies Library Folder",
      "section": "required",
      "input_type": "text",
      "required": true,
      "default": "./movies",
      "help": "Folder where Radarr will organize and manage your movie library."
    },
    {
      "name": "PUID",
      "label": "R5: PUID",
      "section": "required",
      "input_type": "text",
      "required": true,
      "default": "",
      "help": "User ID used by the container for file permissions."
    },
    {
      "name": "PGID",
      "label": "R6: PGID",
      "section": "required",
      "input_type": "text",
      "required": true,
      "default": "",
      "help": "Group ID used by the container for file permissions."
    },
    {
      "name": "TZ",
      "label": "O1: Timezone",
      "section": "optional",
      "input_type": "text",
      "required": true,
      "default": "",
      "help": "Timezone used by Radarr for scheduled tasks and logs.<br>Example: <code>Asia/Dubai</code>."
    },
    {
      "name": "RESTART_POLICY",
      "label": "O2: Restart Policy",
      "section": "optional",
      "input_type": "select",
      "required": true,
      "default": "unless-stopped",
      "options": [
        { "label": "Unless Stopped", "value": "unless-stopped" },
        { "label": "Always", "value": "always" },
        { "label": "On Failure", "value": "on-failure" },
        { "label": "No", "value": "no" }
      ],
      "help": "Controls whether the container restarts automatically."
    },
    {
      "name": "RADARR_NETWORK_MODE",
      "label": "O3: Network",
      "section": "optional",
      "input_type": "select",
      "required": true,
      "default": "__create_bridge__",
      "data_type": "docker_network",
      "options_source": "docker_networks",
      "help": "Choose <code>Create New Bridge</code> for the default Compose network, <code>Host Mode</code> for host networking, or an existing named Docker network."
    },
    {
      "name": "RADARR_IMAGE",
      "label": "A1: Image",
      "section": "advanced",
      "input_type": "text",
      "required": true,
      "default": "lscr.io/linuxserver/radarr:latest",
      "docs_url": "https://docs.linuxserver.io/images/docker-radarr/",
      "docs_label": "Official help",
      "help": "Docker image used for Radarr.<br>Change this only if you want a different tag or image source."
    },
    {
      "name": "RADARR_CONTAINER_NAME",
      "label": "Container Name",
      "section": "readonly",
      "input_type": "text",
      "required": true,
      "default": "${PROJECT_NAME}",
      "editable": false,
      "help": "EasyDocker sets the container name from the project name."
    },
    {
      "name": "RADARR_INTERNAL_PORT",
      "label": "Internal App Port",
      "section": "readonly",
      "input_type": "text",
      "required": true,
      "default": "7878",
      "editable": false,
      "help": "Recipe-managed Radarr internal port."
    },
    {
      "name": "RADARR_CONFIG_TARGET",
      "label": "Config Path Inside Container",
      "section": "readonly",
      "input_type": "text",
      "required": true,
      "default": "/config",
      "editable": false,
      "help": "Recipe-managed internal config path."
    },
    {
      "name": "RADARR_DOWNLOADS_TARGET",
      "label": "Downloads Path Inside Container",
      "section": "readonly",
      "input_type": "text",
      "required": true,
      "default": "/downloads",
      "editable": false,
      "help": "Recipe-managed internal downloads path."
    },
    {
      "name": "RADARR_MOVIES_TARGET",
      "label": "Movies Library Path Inside Container",
      "section": "readonly",
      "input_type": "text",
      "required": true,
      "default": "/movies",
      "editable": false,
      "help": "Recipe-managed internal movies library path."
    }
  ],
  "ui": {
    "manual": "https://tsg-easydocker.com/docs/recipes/radarr/",
    "sections": [
      { "name": "required", "label": "Minimum Required" },
      { "name": "optional", "label": "Optional", "collapsed": true },
      { "name": "advanced", "label": "Advanced", "collapsed": true },
      { "name": "readonly", "label": "Read-Only (Recipe Managed)", "collapsed": true }
    ],
    "tags": [
      "Intel/AMD",
      "ARM"
    ],
    "overview": "Radarr helps you track, download, and organize movies automatically.",
    "what_happens": [
      "A Radarr container will be created",
      "Your config, downloads, and movies library folders will be mounted into the container",
      "You can keep the default bridge network or attach Radarr to an existing network"
    ],
    "data_location": "Relative paths are stored inside this app's project folder under the mounted EasyDocker base config path."
  }
}
